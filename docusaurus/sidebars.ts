import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

/**
* Sidebar definition that mirrors the primary navigation inside Business App.
*
* The Business App sidebar (top-level navigation) excludes the **Home** and
* **My Products** tabs.  All other existing documentation sections are
* displayed in the identical order that users see inside the product.
*
* TOP-LEVEL ORDER
* ───────────────
*   1. Inbox
*   2. CRM
*   3. AI
*   4. Executive Report
*   5. My Business
*   6. Store
*   7. Settings
*   8. Administration
*
* Each top-level item uses **autogenerated** mode so that every markdown file
* inside the matching directory is surfaced automatically. This keeps the file
* system as the single source of truth for sub-pages while still giving us
* fine-grained control over the outer tab order.
*/

const sidebars: SidebarsConfig = {
  tutorialSidebar: [
    // 1. Inbox
    {type: 'autogenerated', dirName: 'inbox'},

    // 2. CRM (all CRM-related guides and sub-pages)
    {
      type: 'category',
      label: 'CRM',
      items: [{type: 'autogenerated', dirName: 'crm'}],
    },

    // 3. AI
    {type: 'autogenerated', dirName: 'ai'},

    // 4. Executive Report
    {type: 'autogenerated', dirName: 'executivereport'},

    // 5. My Business
    {type: 'autogenerated', dirName: 'my-business'},

    // 6. Store
    {type: 'autogenerated', dirName: 'store'},

    // 7. Settings
    {type: 'autogenerated', dirName: 'settings'},

    // 8. Administration
    {type: 'autogenerated', dirName: 'administration'},

    // 🔗 Catch-all
    // Ensures any markdown files placed directly under `docs/` (root) remain
    // reachable in the navigation. Place this **after** the explicit top-level
    // entries so it doesn’t disrupt their order.
    {type: 'autogenerated', dirName: '.'},
  ],
};

export default sidebars;
